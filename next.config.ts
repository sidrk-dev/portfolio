import type { NextConfig } from "next";

const nextConfig = {
  output: "export",
  basePath: "/portfolio",
  images: {
    unoptimized: true,
  },
  /* config options here */
};

export default nextConfig;
